<!DOCTYPE html>
<html lang="en" class="h-full bg-slate-50">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Greater Atlanta 1,200+ Dental Offices Directory & Analytics</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        teal: {
                            50: '#f0fdfa',
                            100: '#ccfbf1',
                            500: '#14b8a6',
                            600: '#0d9488',
                            700: '#0f766e',
                            800: '#115e59',
                            900: '#134e4a',
                        },
                        cyan: {
                            500: '#06b6d4',
                            600: '#0891b2',
                        }
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    }
                }
            }
        }
    </script>

    <!-- FontAwesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Leaflet.js CSS & JS -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

    <!-- Leaflet MarkerCluster CSS & JS -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.4.1/dist/MarkerCluster.css" />
    <link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster@1.4.1/dist/MarkerCluster.Default.css" />
    <script src="https://unpkg.com/leaflet.markercluster@1.4.1/dist/leaflet.markercluster.js"></script>

    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <style>
        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }
        ::-webkit-scrollbar-track {
            background: #f1f5f9;
        }
        ::-webkit-scrollbar-thumb {
            background: #cbd5e1;
            border-radius: 9999px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #94a3b8;
        }

        /* Leaflet Map Styling Override */
        .leaflet-container {
            font-family: inherit;
            border-radius: 0.75rem;
            z-index: 10;
        }
        
        /* Custom Marker Cluster styling */
        .marker-cluster-small {
            background-color: rgba(20, 184, 166, 0.6) !important;
        }
        .marker-cluster-small div {
            background-color: rgba(15, 118, 110, 0.9) !important;
            color: #ffffff !important;
            font-weight: 700;
        }
        .marker-cluster-medium {
            background-color: rgba(14, 116, 144, 0.6) !important;
        }
        .marker-cluster-medium div {
            background-color: rgba(8, 145, 178, 0.9) !important;
            color: #ffffff !important;
            font-weight: 700;
        }
        .marker-cluster-large {
            background-color: rgba(30, 58, 138, 0.6) !important;
        }
        .marker-cluster-large div {
            background-color: rgba(30, 58, 138, 0.9) !important;
            color: #ffffff !important;
            font-weight: 700;
        }

        .custom-map-pin {
            background: #0f766e;
            border: 2px solid #ffffff;
            color: #ffffff;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.2);
            font-size: 11px;
            transition: all 0.2s ease-in-out;
        }
        .custom-map-pin.emergency {
            background: #dc2626;
        }
        .custom-map-pin.forsyth-pin {
            border: 2px solid #f59e0b;
        }

        /* Modal Animation */
        @keyframes modalFadeIn {
            from { opacity: 0; transform: scale(0.96); }
            to { opacity: 1; transform: scale(1); }
        }
        .animate-modal {
            animation: modalFadeIn 0.2s ease-out forwards;
        }
    </style>
</head>
<body class="h-full font-sans text-slate-800 bg-slate-50 flex flex-col overflow-x-hidden">

    <header class="bg-slate-900 text-white border-b border-slate-800 sticky top-0 z-30 shadow-md">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between">
            <div class="flex items-center space-x-3">
                <div class="bg-gradient-to-tr from-teal-500 to-amber-400 w-10 h-10 rounded-xl flex items-center justify-center shadow-md">
                    <i class="fa-solid fa-tooth text-slate-900 text-xl"></i>
                </div>
                <div>
                    <h1 class="text-base sm:text-lg font-bold tracking-tight text-white flex items-center gap-2">
                        ATL Dental Network <span id="header-count-badge" class="text-xs font-semibold px-2 py-0.5 bg-teal-500/20 text-teal-300 rounded-full border border-teal-500/30">1,250 Offices</span>
                    </h1>
                    <p class="text-xs text-slate-400 hidden sm:block">7 Metro Counties: Fulton, Cobb, Gwinnett, DeKalb, Forsyth, Cherokee & Henry</p>
                </div>
            </div>

            <!-- View Switcher Tabs & Reset -->
            <div class="flex items-center space-x-2 sm:space-x-3">
                <div class="bg-slate-800 p-1 rounded-lg flex text-xs font-medium border border-slate-700">
                    <button id="tab-directory-btn" onclick="switchTab('directory')" class="px-3 py-1.5 rounded-md bg-teal-600 text-white font-semibold shadow-sm transition-all flex items-center gap-1.5">
                        <i class="fa-solid fa-map-location-dot"></i>
                        <span class="hidden sm:inline">Finder & Map</span>
                    </button>
                    <button id="tab-analytics-btn" onclick="switchTab('analytics')" class="px-3 py-1.5 rounded-md text-slate-300 hover:text-white hover:bg-slate-700/50 transition-all flex items-center gap-1.5">
                        <i class="fa-solid fa-chart-pie"></i>
                        <span class="hidden sm:inline">Metro Intelligence</span>
                    </button>
                </div>
                
                <button onclick="exportToCSV()" title="Export Filtered CSV" class="p-2 text-slate-400 hover:text-white hover:bg-slate-800 rounded-lg border border-slate-700 transition hidden md:flex items-center gap-1 text-xs font-medium">
                    <i class="fa-solid fa-file-csv text-teal-400"></i>
                    <span>Export</span>
                </button>

                <button onclick="resetAllFilters()" title="Reset Filters" class="p-2 text-slate-400 hover:text-white hover:bg-slate-800 rounded-lg border border-slate-700 transition">
                    <i class="fa-solid fa-rotate-right"></i>
                </button>
            </div>
        </div>
    </header>

    <main class="flex-1 max-w-7xl w-full mx-auto px-4 sm:px-6 lg:px-8 py-5 flex flex-col gap-5">
        
        <!-- KPI Metrics Grid -->
        <section class="grid grid-cols-2 lg:grid-cols-4 gap-4">
            <!-- KPI 1 -->
            <div class="bg-white rounded-xl border border-slate-200 p-4 shadow-sm flex items-center justify-between">
                <div>
                    <p class="text-xs font-medium text-slate-500 uppercase tracking-wider">Total Metro Offices</p>
                    <h3 id="kpi-total" class="text-2xl font-bold text-slate-900 mt-1">1,250</h3>
                    <p id="kpi-total-sub" class="text-xs text-teal-600 font-medium mt-1 flex items-center gap-1">
                        <i class="fa-solid fa-circle-check"></i> 100% Active Index
                    </p>
                </div>
                <div class="w-12 h-12 rounded-xl bg-teal-50 text-teal-600 flex items-center justify-center text-xl font-semibold">
                    <i class="fa-solid fa-hospital"></i>
                </div>
            </div>

            <!-- KPI 2 -->
            <div class="bg-white rounded-xl border border-slate-200 p-4 shadow-sm flex items-center justify-between">
                <div>
                    <p class="text-xs font-medium text-slate-500 uppercase tracking-wider">Average Metro Rating</p>
                    <h3 id="kpi-rating" class="text-2xl font-bold text-slate-900 mt-1">4.82</h3>
                    <p class="text-xs text-amber-500 font-medium mt-1 flex items-center gap-1">
                        <i class="fa-solid fa-star"></i> Based on 340,000+ reviews
                    </p>
                </div>
                <div class="w-12 h-12 rounded-xl bg-amber-50 text-amber-500 flex items-center justify-center text-xl">
                    <i class="fa-solid fa-star"></i>
                </div>
            </div>

            <!-- KPI 3 -->
            <div class="bg-white rounded-xl border border-slate-200 p-4 shadow-sm flex items-center justify-between">
                <div>
                    <p class="text-xs font-medium text-slate-500 uppercase tracking-wider">Emergency Dental</p>
                    <h3 id="kpi-emergency" class="text-2xl font-bold text-slate-900 mt-1">182</h3>
                    <p class="text-xs text-rose-600 font-medium mt-1 flex items-center gap-1">
                        <i class="fa-solid fa-bolt"></i> 24/7 & Urgent Care
                    </p>
                </div>
                <div class="w-12 h-12 rounded-xl bg-rose-50 text-rose-600 flex items-center justify-center text-xl">
                    <i class="fa-solid fa-kit-medical"></i>
                </div>
            </div>

            <!-- KPI 4 -->
            <div class="bg-white rounded-xl border border-slate-200 p-4 shadow-sm flex items-center justify-between">
                <div>
                    <p class="text-xs font-medium text-slate-500 uppercase tracking-wider">Regional Coverage</p>
                    <h3 id="kpi-counties" class="text-2xl font-bold text-slate-900 mt-1">7 Counties</h3>
                    <p class="text-xs text-indigo-600 font-medium mt-1">Fulton, Cobb, Gwinnett, DeKalb, Forsyth, Cherokee, Henry</p>
                </div>
                <div class="w-12 h-12 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center text-xl">
                    <i class="fa-solid fa-map-location-dot"></i>
                </div>
            </div>
        </section>

        <section class="bg-white rounded-xl border border-slate-200 p-4 shadow-sm space-y-4">
            <!-- Row 1: Search + County + Insurance + Min Rating -->
            <div class="grid grid-cols-1 md:grid-cols-12 gap-3 items-center">
                <!-- Search Bar -->
                <div class="md:col-span-4 relative">
                    <i class="fa-solid fa-magnifying-glass absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400 text-sm"></i>
                    <input type="text" id="search-input" onkeyup="handleSearch()" placeholder="Search name, doctor, zip, city (Cumming, Alpharetta, Buckhead, Marietta)..." 
                           class="w-full pl-10 pr-4 py-2 bg-slate-50 border border-slate-300 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-teal-500 focus:bg-white transition" />
                </div>

                <!-- County Dropdown -->
                <div class="md:col-span-3">
                    <select id="county-select" onchange="applyFilters()" class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg text-sm text-slate-700 font-medium focus:outline-none focus:ring-2 focus:ring-teal-500 transition">
                        <option value="ALL">All 7 Metro Counties</option>
                        <option value="Fulton">Fulton County (Buckhead, Midtown, Alpharetta, Roswell, Johns Creek)</option>
                        <option value="Cobb">Cobb County (Marietta, Smyrna, Kennesaw, East Cobb)</option>
                        <option value="Gwinnett">Gwinnett County (Duluth, Lawrenceville, Suwanee, Peachtree Corners)</option>
                        <option value="DeKalb">DeKalb County (Decatur, Dunwoody, Brookhaven, Emory)</option>
                        <option value="Forsyth">Forsyth County (Cumming, Halcyon, Vickery, Windermere)</option>
                        <option value="Cherokee">Cherokee County (Canton, Woodstock, Holly Springs)</option>
                        <option value="Henry">Henry County (McDonough, Stockbridge, Hampton)</option>
                    </select>
                </div>

                <!-- Insurance Dropdown -->
                <div class="md:col-span-3">
                    <select id="insurance-select" onchange="applyFilters()" class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg text-sm text-slate-700 font-medium focus:outline-none focus:ring-2 focus:ring-teal-500 transition">
                        <option value="ALL">All Accepted Insurance Plans</option>
                        <option value="Delta Dental">Delta Dental</option>
                        <option value="Blue Cross Blue Shield">Blue Cross Blue Shield (BCBS)</option>
                        <option value="Cigna">Cigna</option>
                        <option value="MetLife">MetLife</option>
                        <option value="Aetna">Aetna</option>
                        <option value="Guardian">Guardian</option>
                        <option value="Humana">Humana</option>
                        <option value="UnitedHealthcare">UnitedHealthcare</option>
                        <option value="Self-Pay">Self-Pay / Cash Options</option>
                    </select>
                </div>

                <!-- Rating Threshold -->
                <div class="md:col-span-2">
                    <select id="rating-select" onchange="applyFilters()" class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg text-sm text-slate-700 font-medium focus:outline-none focus:ring-2 focus:ring-teal-500 transition">
                        <option value="0">Any Rating</option>
                        <option value="4.5">4.5+ ★ Stars</option>
                        <option value="4.8">4.8+ ★ Stars</option>
                        <option value="4.9">4.9+ ★ Stars</option>
                    </select>
                </div>
            </div>

            <!-- Row 2: Specialty Chips & Feature Toggles -->
            <div class="flex flex-wrap items-center justify-between gap-3 pt-2 border-t border-slate-100">
                <!-- Specialty Pills -->
                <div class="flex flex-wrap items-center gap-1.5" id="specialty-chips">
                    <span class="text-xs font-semibold text-slate-500 mr-1">Specialties:</span>
                    <button onclick="toggleSpecialty('ALL')" data-specialty="ALL" class="chip-btn active-chip px-2.5 py-1 text-xs rounded-full border transition font-medium bg-teal-700 text-white border-teal-700">All</button>
                    <button onclick="toggleSpecialty('General Dentistry')" data-specialty="General Dentistry" class="chip-btn px-2.5 py-1 text-xs rounded-full border bg-slate-100 text-slate-700 border-slate-200 hover:bg-slate-200 transition font-medium">General Dentistry</button>
                    <button onclick="toggleSpecialty('Orthodontics')" data-specialty="Orthodontics" class="chip-btn px-2.5 py-1 text-xs rounded-full border bg-slate-100 text-slate-700 border-slate-200 hover:bg-slate-200 transition font-medium">Orthodontics</button>
                    <button onclick="toggleSpecialty('Pediatric')" data-specialty="Pediatric" class="chip-btn px-2.5 py-1 text-xs rounded-full border bg-slate-100 text-slate-700 border-slate-200 hover:bg-slate-200 transition font-medium">Pediatric</button>
                    <button onclick="toggleSpecialty('Cosmetic')" data-specialty="Cosmetic" class="chip-btn px-2.5 py-1 text-xs rounded-full border bg-slate-100 text-slate-700 border-slate-200 hover:bg-slate-200 transition font-medium">Cosmetic</button>
                    <button onclick="toggleSpecialty('Endodontics/Periodontics')" data-specialty="Endodontics/Periodontics" class="chip-btn px-2.5 py-1 text-xs rounded-full border bg-slate-100 text-slate-700 border-slate-200 hover:bg-slate-200 transition font-medium">Endo / Perio</button>
                    <button onclick="toggleSpecialty('Emergency Oral Surgery')" data-specialty="Emergency Oral Surgery" class="chip-btn px-2.5 py-1 text-xs rounded-full border bg-slate-100 text-slate-700 border-slate-200 hover:bg-slate-200 transition font-medium">Emergency Surgery</button>
                </div>

                <!-- Toggles -->
                <div class="flex flex-wrap items-center gap-4 text-xs font-medium text-slate-700">
                    <label class="inline-flex items-center cursor-pointer space-x-2">
                        <input type="checkbox" id="toggle-emergency" onchange="applyFilters()" class="w-4 h-4 text-rose-600 rounded border-slate-300 focus:ring-rose-500">
                        <span class="flex items-center gap-1 text-rose-700 font-semibold"><i class="fa-solid fa-kit-medical text-rose-500"></i> Emergency 24/7</span>
                    </label>

                    <label class="inline-flex items-center cursor-pointer space-x-2">
                        <input type="checkbox" id="toggle-weekend" onchange="applyFilters()" class="w-4 h-4 text-teal-600 rounded border-slate-300 focus:ring-teal-500">
                        <span><i class="fa-regular fa-calendar-check text-slate-500"></i> Open Weekends</span>
                    </label>

                    <label class="inline-flex items-center cursor-pointer space-x-2">
                        <input type="checkbox" id="toggle-newpatient" onchange="applyFilters()" class="w-4 h-4 text-teal-600 rounded border-slate-300 focus:ring-teal-500">
                        <span><i class="fa-solid fa-user-plus text-slate-500"></i> New Patients Accepted</span>
                    </label>
                </div>
            </div>
        </section>

        <!-- TAB 1: DIRECTORY & MAP VIEW -->
        <div id="view-directory" class="grid grid-cols-1 lg:grid-cols-12 gap-5 flex-1 items-start">
            
            <!-- Left Side: Practice Cards List + Pagination -->
            <div class="lg:col-span-7 flex flex-col space-y-4">
                <!-- Status Header & Sort -->
                <div class="flex flex-wrap items-center justify-between text-xs text-slate-500 px-1 gap-2">
                    <span id="results-count-label" class="font-medium text-slate-700">Showing 1–20 of 1,250 Offices</span>
                    
                    <div class="flex items-center space-x-3">
                        <div class="flex items-center space-x-1">
                            <span>Per page:</span>
                            <select id="pageSize-select" onchange="changePageSize()" class="bg-transparent font-medium text-slate-800 border-b border-slate-300 focus:outline-none cursor-pointer">
                                <option value="20">20</option>
                                <option value="50">50</option>
                                <option value="100">100</option>
                            </select>
                        </div>

                        <div class="flex items-center space-x-1">
                            <span>Sort:</span>
                            <select id="sort-select" onchange="applyFilters()" class="bg-transparent font-medium text-slate-800 border-b border-slate-300 focus:outline-none cursor-pointer">
                                <option value="rating-desc">Highest Rating</option>
                                <option value="reviews-desc">Most Reviews</option>
                                <option value="name-asc">Practice Name (A-Z)</option>
                                <option value="county-asc">County (A-Z)</option>
                            </select>
                        </div>
                    </div>
                </div>

                <!-- Practice Cards List Container -->
                <div id="practice-cards-list" class="space-y-4 min-h-[500px]">
                    <!-- Paginated practice cards injected via JS -->
                </div>

                <!-- Pagination Navigation Controls -->
                <div id="pagination-controls" class="bg-white rounded-xl border border-slate-200 p-3 shadow-sm flex items-center justify-between text-xs font-semibold">
                    <button id="btn-prev-page" onclick="prevPage()" class="px-3.5 py-2 bg-slate-100 hover:bg-slate-200 text-slate-700 rounded-lg transition disabled:opacity-40 disabled:cursor-not-allowed flex items-center gap-1">
                        <i class="fa-solid fa-chevron-left"></i> Previous
                    </button>
                    <span id="pagination-info" class="text-slate-600 font-medium">Page 1 of 63</span>
                    <button id="btn-next-page" onclick="nextPage()" class="px-3.5 py-2 bg-slate-100 hover:bg-slate-200 text-slate-700 rounded-lg transition disabled:opacity-40 disabled:cursor-not-allowed flex items-center gap-1">
                        Next <i class="fa-solid fa-chevron-right"></i>
                    </button>
                </div>
            </div>

            <!-- Right Side: Interactive MarkerCluster Map -->
            <div class="lg:col-span-5 sticky top-20">
                <div class="bg-white rounded-xl border border-slate-200 p-2 shadow-sm flex flex-col">
                    <div class="flex items-center justify-between px-3 py-2 border-b border-slate-100">
                        <div class="flex items-center space-x-2 text-xs font-semibold text-slate-700 uppercase tracking-wide">
                            <i class="fa-solid fa-map-pin text-teal-600"></i>
                            <span>7-County Cluster Map (1,250 Locations)</span>
                        </div>
                        <button onclick="recenterMap()" class="text-xs text-teal-600 hover:text-teal-800 font-medium flex items-center gap-1">
                            <i class="fa-solid fa-crosshairs"></i> Fit All Offices
                        </button>
                    </div>
                    <div id="map" class="w-full h-[640px] rounded-lg mt-2"></div>
                </div>
            </div>
        </div>

        <!-- TAB 2: METRO ANALYTICS VIEW -->
        <div id="view-analytics" class="hidden space-y-6">
            <div class="bg-white rounded-xl border border-slate-200 p-4 shadow-sm flex flex-wrap items-center justify-between gap-3">
                <div>
                    <h2 class="text-base font-bold text-slate-900 flex items-center gap-2">
                        <i class="fa-solid fa-chart-line text-teal-600"></i> Greater Atlanta Dental Market Intelligence
                    </h2>
                    <p class="text-xs text-slate-500 mt-0.5">Real-time aggregate data calculated from 1,250+ dental practices across Fulton, Cobb, Gwinnett, DeKalb, Forsyth, Cherokee, and Henry counties.</p>
                </div>
                <div class="text-xs bg-teal-50 border border-teal-200 text-teal-800 font-semibold px-3 py-1.5 rounded-lg flex items-center gap-2">
                    <i class="fa-solid fa-database"></i> Live Dataset Sync
                </div>
            </div>

            <!-- Analytics Charts Grid -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Chart 1: Practice Count by County -->
                <div class="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
                    <h3 class="text-sm font-semibold text-slate-800 mb-4 flex items-center gap-2">
                        <i class="fa-solid fa-chart-bar text-teal-600"></i> Practice Distribution across 7 Counties
                    </h3>
                    <div class="h-64 relative">
                        <canvas id="chart-county-dist"></canvas>
                    </div>
                </div>

                <!-- Chart 2: Average Rating by County -->
                <div class="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
                    <h3 class="text-sm font-semibold text-slate-800 mb-4 flex items-center gap-2">
                        <i class="fa-solid fa-star text-amber-500"></i> Average Patient Rating by County
                    </h3>
                    <div class="h-64 relative">
                        <canvas id="chart-county-ratings"></canvas>
                    </div>
                </div>

                <!-- Chart 3: Top Accepted Insurances -->
                <div class="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
                    <h3 class="text-sm font-semibold text-slate-800 mb-4 flex items-center gap-2">
                        <i class="fa-solid fa-shield-halved text-cyan-600"></i> Insurance Network Participation
                    </h3>
                    <div class="h-64 relative">
                        <canvas id="chart-insurance"></canvas>
                    </div>
                </div>

                <!-- Chart 4: Service Availability Features -->
                <div class="bg-white p-5 rounded-xl border border-slate-200 shadow-sm">
                    <h3 class="text-sm font-semibold text-slate-800 mb-4 flex items-center gap-2">
                        <i class="fa-solid fa-clock text-rose-500"></i> Service & Emergency Availability Rates
                    </h3>
                    <div class="h-64 relative">
                        <canvas id="chart-availability"></canvas>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- PRACTICE DETAILS MODAL -->
    <div id="detail-modal" class="fixed inset-0 z-50 hidden bg-slate-900/60 backdrop-blur-sm flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl max-w-3xl w-full max-h-[90vh] overflow-y-auto shadow-2xl border border-slate-200 animate-modal flex flex-col">
            <!-- Modal Header -->
            <div class="p-6 bg-slate-900 text-white flex items-start justify-between rounded-t-2xl relative">
                <div>
                    <span id="modal-county-badge" class="px-2.5 py-0.5 rounded-full text-xs font-semibold bg-teal-500/20 text-teal-300 border border-teal-500/30">
                        Forsyth County
                    </span>
                    <h2 id="modal-title" class="text-xl sm:text-2xl font-bold text-white mt-1">Forsyth Premier Dental & Orthodontics</h2>
                    <p id="modal-address" class="text-xs text-slate-300 mt-1 flex items-center gap-1.5">
                        <i class="fa-solid fa-location-dot text-teal-400"></i> 6345 Halcyon Way, Suite 210, Cumming, GA 30041
                    </p>
                </div>
                <button onclick="closeDetailModal()" class="text-slate-400 hover:text-white bg-slate-800 p-2 rounded-full transition">
                    <i class="fa-solid fa-xmark text-lg"></i>
                </button>
            </div>

            <!-- Modal Body -->
            <div class="p-6 space-y-6 flex-1">
                <!-- Status & KPI Strip -->
                <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 bg-slate-50 p-3 rounded-xl border border-slate-200 text-center">
                    <div>
                        <span class="text-xs text-slate-500 block">Rating</span>
                        <span id="modal-rating" class="text-lg font-bold text-amber-600 flex items-center justify-center gap-1">
                            4.9 <i class="fa-solid fa-star text-sm"></i>
                        </span>
                    </div>
                    <div>
                        <span class="text-xs text-slate-500 block">Patient Reviews</span>
                        <span id="modal-reviews" class="text-lg font-bold text-slate-800">384</span>
                    </div>
                    <div>
                        <span class="text-xs text-slate-500 block">Operating Hours</span>
                        <span id="modal-status" class="text-sm font-semibold text-emerald-600">Open Now</span>
                    </div>
                    <div>
                        <span class="text-xs text-slate-500 block">Direct Phone</span>
                        <span id="modal-phone" class="text-xs font-semibold text-slate-800 block mt-1">(770) 887-2020</span>
                    </div>
                </div>

                <!-- Tabs in Modal -->
                <div class="border-b border-slate-200 flex space-x-6 text-sm font-medium">
                    <button onclick="switchModalTab('doctors')" id="mtab-doctors-btn" class="py-2 border-b-2 border-teal-600 text-teal-700 font-semibold">Dentists & Staff</button>
                    <button onclick="switchModalTab('services')" id="mtab-services-btn" class="py-2 border-b-2 border-transparent text-slate-500 hover:text-slate-700">Procedure Pricing</button>
                    <button onclick="switchModalTab('hours')" id="mtab-hours-btn" class="py-2 border-b-2 border-transparent text-slate-500 hover:text-slate-700">Hours & Insurance</button>
                </div>

                <!-- Doctor Staff Section -->
                <div id="mtab-doctors" class="space-y-3">
                    <h4 class="text-xs font-semibold uppercase tracking-wider text-slate-400">Practicing Dental Specialists</h4>
                    <div id="modal-doctors-list" class="grid grid-cols-1 sm:grid-cols-2 gap-3">
                        <!-- Doctors inserted via JS -->
                    </div>
                </div>

                <!-- Procedure Costs Section -->
                <div id="mtab-services" class="hidden space-y-3">
                    <h4 class="text-xs font-semibold uppercase tracking-wider text-slate-400">Estimated Procedure Pricing Guide</h4>
                    <div class="border border-slate-200 rounded-lg overflow-hidden">
                        <table class="w-full text-left text-xs">
                            <thead class="bg-slate-100 text-slate-700 font-semibold border-b border-slate-200">
                                <tr>
                                    <th class="p-2.5">Procedure Description</th>
                                    <th class="p-2.5">Category</th>
                                    <th class="p-2.5 text-right">Est. Out-of-Pocket</th>
                                </tr>
                            </thead>
                            <tbody id="modal-services-table" class="divide-y divide-slate-100 text-slate-600">
                                <!-- Services injected via JS -->
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- Hours & Insurance Section -->
                <div id="mtab-hours" class="hidden space-y-4">
                    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                        <div>
                            <h4 class="text-xs font-semibold uppercase tracking-wider text-slate-400 mb-2">Weekly Schedule</h4>
                            <ul id="modal-hours-list" class="space-y-1 text-xs text-slate-600 font-medium">
                                <!-- Hours injected -->
                            </ul>
                        </div>
                        <div>
                            <h4 class="text-xs font-semibold uppercase tracking-wider text-slate-400 mb-2">Accepted Insurance Carriers</h4>
                            <div id="modal-insurance-pills" class="flex flex-wrap gap-1.5">
                                <!-- Insurance pills injected -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Modal Footer Actions -->
            <div class="p-4 bg-slate-50 border-t border-slate-200 flex items-center justify-between rounded-b-2xl">
                <div class="flex items-center space-x-2">
                    <button onclick="copyAddressToClipboard()" class="text-xs text-slate-600 hover:text-slate-900 font-medium flex items-center gap-1.5 px-3 py-2 rounded-lg border border-slate-300 bg-white">
                        <i class="fa-solid fa-copy"></i> Copy Address
                    </button>
                    <button onclick="jumpToModalMarker()" class="text-xs text-teal-700 hover:text-teal-900 font-medium flex items-center gap-1.5 px-3 py-2 rounded-lg border border-teal-200 bg-teal-50">
                        <i class="fa-solid fa-location-crosshairs"></i> View on Map
                    </button>
                </div>
                <button id="modal-book-btn" onclick="openBookingModal()" class="px-5 py-2.5 bg-teal-600 hover:bg-teal-700 text-white rounded-lg font-semibold text-sm shadow flex items-center gap-2">
                    <i class="fa-solid fa-calendar-check"></i> Book Appointment
                </button>
            </div>
        </div>
    </div>

    <!-- APPOINTMENT BOOKING MODAL -->
    <div id="booking-modal" class="fixed inset-0 z-50 hidden bg-slate-900/60 backdrop-blur-sm flex items-center justify-center p-4">
        <div class="bg-white rounded-2xl max-w-lg w-full p-6 shadow-2xl border border-slate-200 animate-modal">
            <div class="flex items-center justify-between border-b border-slate-100 pb-4 mb-4">
                <div>
                    <h3 class="text-lg font-bold text-slate-900">Schedule Dental Visit</h3>
                    <p id="booking-practice-name" class="text-xs text-teal-600 font-semibold">Forsyth Premier Dental & Orthodontics</p>
                </div>
                <button onclick="closeBookingModal()" class="text-slate-400 hover:text-slate-600">
                    <i class="fa-solid fa-xmark text-lg"></i>
                </button>
            </div>

            <form id="booking-form" onsubmit="handleBookingSubmit(event)" class="space-y-3 text-xs">
                <div>
                    <label class="block font-medium text-slate-700 mb-1">Patient Full Name</label>
                    <input type="text" id="book-name" required placeholder="e.g., Sarah Jenkins" class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500">
                </div>

                <div class="grid grid-cols-2 gap-3">
                    <div>
                        <label class="block font-medium text-slate-700 mb-1">Phone Number</label>
                        <input type="tel" id="book-phone" required placeholder="(770) 555-0199" class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500">
                    </div>
                    <div>
                        <label class="block font-medium text-slate-700 mb-1">Email Address</label>
                        <input type="email" id="book-email" required placeholder="sarah@example.com" class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500">
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-3">
                    <div>
                        <label class="block font-medium text-slate-700 mb-1">Preferred Date</label>
                        <input type="date" id="book-date" required class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500">
                    </div>
                    <div>
                        <label class="block font-medium text-slate-700 mb-1">Preferred Time Slot</label>
                        <select id="book-time" required class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500">
                            <option value="">Select Time</option>
                            <option value="morning">Morning (8:00 AM - 11:30 AM)</option>
                            <option value="afternoon">Afternoon (1:00 PM - 4:00 PM)</option>
                            <option value="evening">Late Afternoon (4:00 PM - 6:00 PM)</option>
                        </select>
                    </div>
                </div>

                <div>
                    <label class="block font-medium text-slate-700 mb-1">Procedure Type</label>
                    <select id="book-reason" required class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500">
                        <option value="Cleaning & Exam">Routine Cleaning & Exam</option>
                        <option value="Emergency Toothache">Urgent Toothache / Emergency Care</option>
                        <option value="Teeth Whitening">Teeth Whitening & Cosmetic Consult</option>
                        <option value="Orthodontics">Braces / Invisalign Assessment</option>
                        <option value="Filling / Crown">Filling, Root Canal or Crown</option>
                        <option value="Pediatric Checkup">Pediatric Child Dental Checkup</option>
                        <option value="Implants">Dental Implant Consultation</option>
                    </select>
                </div>

                <div>
                    <label class="block font-medium text-slate-700 mb-1">Insurance Provider</label>
                    <select id="book-insurance" class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500">
                        <option value="Delta Dental">Delta Dental</option>
                        <option value="Blue Cross Blue Shield">Blue Cross Blue Shield (BCBS)</option>
                        <option value="Cigna">Cigna</option>
                        <option value="MetLife">MetLife</option>
                        <option value="Aetna">Aetna</option>
                        <option value="Guardian">Guardian</option>
                        <option value="Humana">Humana</option>
                        <option value="UnitedHealthcare">UnitedHealthcare</option>
                        <option value="Self-Pay">Self-Pay / Cash Option</option>
                    </select>
                </div>

                <div>
                    <label class="block font-medium text-slate-700 mb-1">Additional Notes (Optional)</label>
                    <textarea id="book-notes" rows="2" placeholder="Mention any specific symptoms or preferences..." class="w-full px-3 py-2 bg-slate-50 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-teal-500"></textarea>
                </div>

                <div class="pt-2 flex items-center justify-end space-x-2">
                    <button type="button" onclick="closeBookingModal()" class="px-4 py-2 border border-slate-300 rounded-lg text-slate-600 hover:bg-slate-100 font-medium">Cancel</button>
                    <button type="submit" class="px-5 py-2 bg-teal-600 hover:bg-teal-700 text-white font-semibold rounded-lg shadow">Confirm Request</button>
                </div>
            </form>
        </div>
    </div>

    <!-- TOAST NOTIFICATIONS -->
    <div id="toast-container" class="fixed bottom-5 right-5 z-50 flex flex-col space-y-2 pointer-events-none"></div>

    <script>
        /**
         * GREATER ATLANTA METRO AREA DATASET GENERATOR (~1,250 REALISTIC PRACTICES)
         */
        const TOTAL_PRACTICES_TARGET = 1250;

        // County Geographic Bounds & Cities Mapping
        const metroCountyConfigs = [
            {
                county: "Fulton",
                weight: 0.30, // 30% of practices
                cities: [
                    { name: "Midtown Atlanta", zip: "30309", latMin: 33.775, latMax: 33.795, lngMin: -84.390, lngMax: -84.370 },
                    { name: "Buckhead", zip: "30326", latMin: 33.835, latMax: 33.860, lngMin: -84.390, lngMax: -84.360 },
                    { name: "Alpharetta", zip: "30005", latMin: 34.060, latMax: 34.095, lngMin: -84.310, lngMax: -84.260 },
                    { name: "Roswell", zip: "30075", latMin: 34.015, latMax: 34.055, lngMin: -84.380, lngMax: -84.330 },
                    { name: "Johns Creek", zip: "30097", latMin: 34.010, latMax: 34.050, lngMin: -84.220, lngMax: -84.170 },
                    { name: "Sandy Springs", zip: "30328", latMin: 33.910, latMax: 33.950, lngMin: -84.380, lngMax: -84.340 }
                ]
            },
            {
                county: "Cobb",
                weight: 0.20,
                cities: [
                    { name: "Marietta", zip: "30060", latMin: 33.940, latMax: 33.980, lngMin: -84.560, lngMax: -84.500 },
                    { name: "Smyrna", zip: "30080", latMin: 33.870, latMax: 33.910, lngMin: -84.530, lngMax: -84.480 },
                    { name: "Kennesaw", zip: "30144", latMin: 34.010, latMax: 34.050, lngMin: -84.630, lngMax: -84.580 },
                    { name: "East Cobb", zip: "30068", latMin: 33.960, latMax: 34.000, lngMin: -84.460, lngMax: -84.400 }
                ]
            },
            {
                county: "Gwinnett",
                weight: 0.20,
                cities: [
                    { name: "Duluth", zip: "30096", latMin: 33.990, latMax: 34.030, lngMin: -84.160, lngMax: -84.110 },
                    { name: "Lawrenceville", zip: "30046", latMin: 33.930, latMax: 33.970, lngMin: -84.020, lngMax: -83.960 },
                    { name: "Suwanee", zip: "30024", latMin: 34.030, latMax: 34.070, lngMin: -84.100, lngMax: -84.040 },
                    { name: "Peachtree Corners", zip: "30092", latMin: 33.950, latMax: 33.985, lngMin: -84.230, lngMax: -84.190 }
                ]
            },
            {
                county: "DeKalb",
                weight: 0.12,
                cities: [
                    { name: "Decatur", zip: "30030", latMin: 33.760, latMax: 33.790, lngMin: -84.310, lngMax: -84.270 },
                    { name: "Dunwoody", zip: "30338", latMin: 33.930, latMax: 33.960, lngMin: -84.330, lngMax: -84.290 },
                    { name: "Brookhaven", zip: "30319", latMin: 33.850, latMax: 33.885, lngMin: -84.340, lngMax: -84.310 },
                    { name: "Tucker", zip: "30084", latMin: 33.830, latMax: 33.865, lngMin: -84.230, lngMax: -84.190 }
                ]
            },
            {
                county: "Forsyth",
                weight: 0.08,
                cities: [
                    { name: "Cumming", zip: "30040", latMin: 34.190, latMax: 34.230, lngMin: -84.160, lngMax: -84.110 },
                    { name: "Halcyon / South Forsyth", zip: "30041", latMin: 34.100, latMax: 34.140, lngMin: -84.240, lngMax: -84.190 },
                    { name: "Vickery Village", zip: "30040", latMin: 34.180, latMax: 34.210, lngMin: -84.240, lngMax: -84.200 },
                    { name: "Windermere", zip: "30041", latMin: 34.130, latMax: 34.160, lngMin: -84.140, lngMax: -84.100 }
                ]
            },
            {
                county: "Cherokee",
                weight: 0.05,
                cities: [
                    { name: "Canton", zip: "30114", latMin: 34.210, latMax: 34.250, lngMin: -84.500, lngMax: -84.440 },
                    { name: "Woodstock", zip: "30188", latMin: 34.090, latMax: 34.130, lngMin: -84.530, lngMax: -84.480 }
                ]
            },
            {
                county: "Henry",
                weight: 0.05,
                cities: [
                    { name: "McDonough", zip: "30253", latMin: 33.430, latMax: 33.470, lngMin: -84.170, lngMax: -84.120 },
                    { name: "Stockbridge", zip: "30281", latMin: 33.520, latMax: 33.560, lngMin: -84.240, lngMax: -84.180 }
                ]
            }
        ];

        const practiceNamePrefixes = ["Peachtree", "Atlanta", "Piedmont", "Heritage", "Southern", "North Metro", "Cumberland", "Cascade", "Emory", "Paces Ferry", "Halcyon", "Windermere", "Towne Lake", "Eagle's Landing", "Apex", "Summit", "Radiant", "Precision", "Signature", "Elite", "ProCare", "Gentle Care", "Cornerstone", "Magnolia", "Dogwood", "Peach State"];
        const practiceNameSuffixes = ["Dental Group", "Family Dentistry", "Smiles Studio", "Dental Associates", "Orthodontics & Implants", "Cosmetic Dentistry", "Pediatric Dental Center", "Dental Care Center", "Urgent Dental & Oral Surgery", "Dental Aesthetics", "Complete Health Dentistry"];

        const doctorFirstNames = ["Ethan", "Sarah", "Marcus", "Rachel", "Andrew", "Elena", "Jonathan", "Maya", "Claire", "Michael", "Robert", "Amanda", "Nathan", "Priya", "Carlos", "Samuel", "Victoria", "David", "Jessica", "James", "Laura", "Kevin", "Stephanie", "Daniel", "Samantha", "Christopher"];
        const doctorLastNames = ["Vance", "Jenkins", "Thorne", "Sterling", "Lin", "Rostova", "Hayes", "Jackson", "Chen", "Bradley", "Ross", "Wright", "Patel", "Gomez", "O'Connor", "Zhang", "Miller", "Kim", "Walker", "Davis", "Taylor", "Anderson", "Thomas", "White", "Harris"];

        const availableSpecialties = ["General Dentistry", "Orthodontics", "Pediatric", "Cosmetic", "Endodontics/Periodontics", "Emergency Oral Surgery"];
        const availableInsurances = ["Delta Dental", "Blue Cross Blue Shield", "Cigna", "MetLife", "Aetna", "Guardian", "Humana", "UnitedHealthcare", "Self-Pay"];

        // Seedable pseudo random generator for reproducible data
        function pseudoRandom(seed) {
            let x = Math.sin(seed++) * 10000;
            return x - Math.floor(x);
        }

        function generate1250Practices() {
            const practices = [];
            let seed = 42;

            for (let i = 1; i <= TOTAL_PRACTICES_TARGET; i++) {
                // Select County based on weights
                const randVal = pseudoRandom(seed++);
                let cumulativeWeight = 0;
                let selectedCountyConfig = metroCountyConfigs[0];
                for (const cfg of metroCountyConfigs) {
                    cumulativeWeight += cfg.weight;
                    if (randVal <= cumulativeWeight) {
                        selectedCountyConfig = cfg;
                        break;
                    }
                }

                // Select City within County
                const cityIndex = Math.floor(pseudoRandom(seed++) * selectedCountyConfig.cities.length);
                const cityObj = selectedCountyConfig.cities[cityIndex];

                // Coordinates
                const lat = +(cityObj.latMin + pseudoRandom(seed++) * (cityObj.latMax - cityObj.latMin)).toFixed(4);
                const lng = +(cityObj.lngMin + pseudoRandom(seed++) * (cityObj.lngMax - cityObj.lngMin)).toFixed(4);

                // Practice Name
                const prefix = practiceNamePrefixes[Math.floor(pseudoRandom(seed++) * practiceNamePrefixes.length)];
                const suffix = practiceNameSuffixes[Math.floor(pseudoRandom(seed++) * practiceNameSuffixes.length)];
                const name = `${prefix} ${suffix}`;

                // Street Address
                const streetNum = Math.floor(pseudoRandom(seed++) * 9000) + 100;
                const streetNames = ["Peachtree Rd", "Main St", "Old Milton Pkwy", "Cobb Pkwy", "Peachtree Corners Cir", "Ponce de Leon Ave", "Cumming Hwy", "Halcyon Way", "Medlock Bridge Rd", "Clifton Rd", "Cherokee St", "Duluth Hwy"];
                const street = streetNames[Math.floor(pseudoRandom(seed++) * streetNames.length)];
                const suite = pseudoRandom(seed++) > 0.4 ? `, Suite ${Math.floor(pseudoRandom(seed++) * 400) + 100}` : '';
                const address = `${streetNum} ${street}${suite}, ${cityObj.name}, GA ${cityObj.zip}`;

                // Phone
                const areaCodes = ["404", "770", "678", "470"];
                const areaCode = areaCodes[Math.floor(pseudoRandom(seed++) * areaCodes.length)];
                const phone = `(${areaCode}) ${Math.floor(pseudoRandom(seed++) * 800 + 100)}-${Math.floor(pseudoRandom(seed++) * 9000 + 1000)}`;

                // Ratings & Reviews
                const rating = +(4.2 + pseudoRandom(seed++) * 0.79).toFixed(1);
                const reviews = Math.floor(pseudoRandom(seed++) * 800) + 25;

                // Flags
                const emergency = pseudoRandom(seed++) < 0.15; // 15% 24/7 emergency
                const weekend = pseudoRandom(seed++) < 0.35; // 35% weekend open
                const acceptingNew = pseudoRandom(seed++) < 0.92; // 92% accepting new

                // Specialties
                const numSpecs = Math.floor(pseudoRandom(seed++) * 3) + 1;
                const specsSet = new Set();
                if (emergency) specsSet.add("Emergency Oral Surgery");
                while (specsSet.size < numSpecs) {
                    specsSet.add(availableSpecialties[Math.floor(pseudoRandom(seed++) * availableSpecialties.length)]);
                }
                const specialties = Array.from(specsSet);

                // Insurances
                const numIns = Math.floor(pseudoRandom(seed++) * 4) + 3;
                const insSet = new Set(["Delta Dental", "Blue Cross Blue Shield"]);
                while (insSet.size < numIns) {
                    insSet.add(availableInsurances[Math.floor(pseudoRandom(seed++) * availableInsurances.length)]);
                }
                const insurances = Array.from(insSet);

                // Doctors
                const numDocs = Math.floor(pseudoRandom(seed++) * 2) + 1;
                const doctors = [];
                for (let d = 0; d < numDocs; d++) {
                    const fname = doctorFirstNames[Math.floor(pseudoRandom(seed++) * doctorFirstNames.length)];
                    const lname = doctorLastNames[Math.floor(pseudoRandom(seed++) * doctorLastNames.length)];
                    const degree = pseudoRandom(seed++) > 0.5 ? "DMD" : "DDS";
                    const exp = Math.floor(pseudoRandom(seed++) * 22) + 4;
                    doctors.push({
                        name: `Dr. ${fname} ${lname}, ${degree}`,
                        role: specialties[d % specialties.length] + " Specialist",
                        experience: `${exp} yrs`
                    });
                }

                // Hours
                const status = emergency ? "Emergency 24/7" : (pseudoRandom(seed++) > 0.2 ? "Open Now" : "Closed Now");

                practices.push({
                    id: `prac_${i}`,
                    name,
                    county: selectedCountyConfig.county,
                    neighborhood: cityObj.name,
                    zip: cityObj.zip,
                    address,
                    lat,
                    lng,
                    phone,
                    rating,
                    reviews,
                    emergency,
                    weekend,
                    acceptingNew,
                    status,
                    specialties,
                    insurances,
                    doctors,
                    hours: {
                        Mon: emergency ? "24 Hours" : "7:30 AM - 5:30 PM",
                        Tue: emergency ? "24 Hours" : "7:30 AM - 5:30 PM",
                        Wed: emergency ? "24 Hours" : "7:30 AM - 5:30 PM",
                        Thu: emergency ? "24 Hours" : "7:30 AM - 5:30 PM",
                        Fri: emergency ? "24 Hours" : "8:00 AM - 4:00 PM",
                        Sat: weekend ? "8:30 AM - 1:30 PM" : "Closed",
                        Sun: emergency ? "24 Hours" : "Closed"
                    },
                    procedures: [
                        { name: "Comprehensive Dental Exam & Cleaning", category: "Preventative", cost: "$110 - $175" },
                        { name: "Digital 3D X-Ray Diagnostic Scan", category: "Diagnostic", cost: "$85 - $140" },
                        { name: "Composite Tooth Filling", category: "Restorative", cost: "$140 - $260" },
                        { name: "Same-Day Porcelain Crown", category: "Restorative", cost: "$950 - $1,450" }
                    ]
                });
            }

            return practices;
        }

        // Generate Full Dataset
        const allDentalPractices = generate1250Practices();

        // Global State
        let filteredPractices = [...allDentalPractices];
        let currentPage = 1;
        let pageSize = 20;
        let currentSpecialtyFilter = "ALL";
        let activePracticeId = null;
        let activeModalPractice = null;
        let bookmarkedPractices = new Set();

        // Leaflet Map & Marker Cluster Instance
        let map = null;
        let markerClusterGroup = null;
        let markersMap = {};

        // Initialize High-Performance Leaflet Map with Marker Clustering
        function initMap() {
            // Metro Atlanta Central View Point
            map = L.map('map', { preferCanvas: true }).setView([33.95, -84.35], 9.5);

            // OpenStreetMap Tiles
            L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
                maxZoom: 18,
                attribution: '&copy; OpenStreetMap contributors'
            }).addTo(map);

            // Initialize MarkerCluster Group
            markerClusterGroup = L.markerClusterGroup({
                chunkedLoading: true,
                maxClusterRadius: 50,
                spiderfyOnMaxZoom: true,
                showCoverageOnHover: false,
                zoomToBoundsOnClick: true
            });

            map.addLayer(markerClusterGroup);
            updateMapMarkers(filteredPractices);
        }

        // Update Leaflet Map Markers based on current filtered dataset
        function updateMapMarkers(practices) {
            if (!markerClusterGroup) return;

            markerClusterGroup.clearLayers();
            markersMap = {};

            const newMarkers = [];

            practices.forEach(p => {
                const isEmergency = p.emergency;
                const isForsyth = p.county === "Forsyth";

                const customIcon = L.divIcon({
                    className: 'custom-pin-wrapper',
                    html: `<div class="custom-map-pin ${isEmergency ? 'emergency' : ''} ${isForsyth ? 'forsyth-pin' : ''}" style="width: 28px; height: 28px;">
                            <i class="fa-solid ${isEmergency ? 'fa-kit-medical' : 'fa-tooth'}"></i>
                          </div>`,
                    iconSize: [28, 28],
                    iconAnchor: [14, 14]
                });

                const marker = L.marker([p.lat, p.lng], { icon: customIcon });

                // Popup view
                const popupContent = `
                    <div class="p-1 space-y-1 max-w-[200px]">
                        <span class="text-[10px] uppercase tracking-wide font-bold ${isForsyth ? 'text-amber-700' : 'text-teal-700'}">${p.county} Co. (${p.neighborhood})</span>
                        <h4 class="font-bold text-xs text-slate-900 leading-tight">${p.name}</h4>
                        <div class="flex items-center text-xs text-amber-500 font-bold">
                            ★ ${p.rating} <span class="text-slate-400 font-normal ml-1">(${p.reviews} reviews)</span>
                        </div>
                        <p class="text-[11px] text-slate-500 leading-tight">${p.address}</p>
                        <button onclick="openDetailModal('${p.id}')" class="mt-2 w-full text-center py-1 bg-teal-600 text-white text-[11px] rounded font-semibold hover:bg-teal-700">
                            View Details
                        </button>
                    </div>
                `;

                marker.bindPopup(popupContent);
                marker.on('click', () => {
                    highlightCard(p.id);
                });

                markersMap[p.id] = marker;
                newMarkers.push(marker);
            });

            markerClusterGroup.addLayers(newMarkers);
        }

        function recenterMap() {
            if (map && markerClusterGroup && filteredPractices.length > 0) {
                map.fitBounds(markerClusterGroup.getBounds(), { padding: [30, 30] });
            }
        }

        // Render Paginated Cards View
        function renderPaginatedCards() {
            const container = document.getElementById('practice-cards-list');
            const total = filteredPractices.length;
            const totalPages = Math.ceil(total / pageSize) || 1;

            if (currentPage > totalPages) currentPage = totalPages;
            if (currentPage < 1) currentPage = 1;

            const startIndex = (currentPage - 1) * pageSize;
            const endIndex = Math.min(startIndex + pageSize, total);
            const pagePractices = filteredPractices.slice(startIndex, endIndex);

            // Label
            document.getElementById('results-count-label').innerText = total > 0 
                ? `Showing ${startIndex + 1}–${endIndex} of ${total.toLocaleString()} Offices`
                : `Showing 0 Offices`;
            document.getElementById('pagination-info').innerText = `Page ${currentPage} of ${totalPages.toLocaleString()}`;

            // Buttons state
            document.getElementById('btn-prev-page').disabled = (currentPage === 1);
            document.getElementById('btn-next-page').disabled = (currentPage === totalPages || total === 0);

            if (total === 0) {
                container.innerHTML = `
                    <div class="bg-white rounded-xl border border-slate-200 p-8 text-center space-y-3">
                        <div class="w-12 h-12 rounded-full bg-slate-100 text-slate-400 flex items-center justify-center mx-auto text-xl">
                            <i class="fa-solid fa-tooth"></i>
                        </div>
                        <h3 class="text-base font-bold text-slate-800">No Dental Offices Match Filter</h3>
                        <p class="text-xs text-slate-500 max-w-sm mx-auto">Try broadening your search query or resetting filters across Fulton, Cobb, Gwinnett, DeKalb, Forsyth, Cherokee, and Henry counties.</p>
                        <button onclick="resetAllFilters()" class="px-4 py-2 text-xs bg-teal-600 text-white rounded-lg font-semibold hover:bg-teal-700 transition">
                            Reset All Filters
                        </button>
                    </div>
                `;
                return;
            }

            container.innerHTML = pagePractices.map(p => {
                const isBookmarked = bookmarkedPractices.has(p.id);
                const isForsyth = p.county === "Forsyth";

                return `
                <div id="card-${p.id}" onclick="selectPracticeCard('${p.id}')" 
                     class="bg-white rounded-xl border border-slate-200 p-4 shadow-sm hover:shadow-md transition-all duration-150 cursor-pointer relative flex flex-col justify-between group">
                    
                    <div>
                        <!-- Header Row -->
                        <div class="flex items-start justify-between gap-2">
                            <div>
                                <div class="flex flex-wrap items-center gap-1.5">
                                    <span class="px-2 py-0.5 rounded text-[10px] font-bold uppercase tracking-wider ${isForsyth ? 'bg-amber-100 text-amber-800 border border-amber-300' : 'bg-slate-100 text-slate-600 border border-slate-200'}">
                                        ${p.county} Co. • ${p.neighborhood}
                                    </span>
                                    ${p.emergency ? `<span class="px-2 py-0.5 rounded text-[10px] font-bold bg-rose-50 text-rose-700 border border-rose-200 flex items-center gap-1"><i class="fa-solid fa-kit-medical"></i> 24/7 Emergency</span>` : ''}
                                </div>
                                <h3 class="text-base font-bold text-slate-900 group-hover:text-teal-700 transition mt-1.5">${p.name}</h3>
                                <p class="text-xs text-slate-500 flex items-center gap-1 mt-0.5">
                                    <i class="fa-solid fa-location-dot text-slate-400"></i> ${p.address}
                                </p>
                            </div>

                            <!-- Rating & Bookmark -->
                            <div class="flex flex-col items-end space-y-1">
                                <button onclick="event.stopPropagation(); toggleBookmark('${p.id}')" title="Bookmark Practice" class="text-slate-300 hover:text-amber-500 text-lg transition">
                                    <i class="${isBookmarked ? 'fa-solid text-amber-500' : 'fa-regular'} fa-bookmark"></i>
                                </button>
                                <div class="bg-amber-50 text-amber-700 border border-amber-200/60 px-2 py-1 rounded-lg flex items-center space-x-1 text-xs font-bold">
                                    <span>${p.rating}</span>
                                    <i class="fa-solid fa-star text-amber-500 text-[10px]"></i>
                                    <span class="text-slate-400 font-normal">(${p.reviews})</span>
                                </div>
                            </div>
                        </div>

                        <!-- Specialties Pills -->
                        <div class="flex flex-wrap gap-1.5 mt-3">
                            ${p.specialties.map(s => `<span class="px-2 py-0.5 bg-teal-50 text-teal-800 border border-teal-200/60 rounded-full text-[11px] font-medium">${s}</span>`).join('')}
                        </div>

                        <!-- Insurance Network Pills -->
                        <div class="mt-2.5 flex items-center gap-1.5 text-xs text-slate-500">
                            <span class="font-semibold text-slate-600 text-[11px]">Accepted:</span>
                            <div class="flex flex-wrap gap-1">
                                ${p.insurances.slice(0, 4).map(i => `<span class="bg-slate-100 text-slate-600 px-1.5 py-0.5 rounded text-[10px]">${i}</span>`).join('')}
                                ${p.insurances.length > 4 ? `<span class="text-[10px] text-slate-400 font-medium">+${p.insurances.length - 4} more</span>` : ''}
                            </div>
                        </div>
                    </div>

                    <!-- Footer Row Actions -->
                    <div class="mt-4 pt-3 border-t border-slate-100 flex items-center justify-between">
                        <div class="flex items-center gap-2 text-xs">
                            <span class="inline-block w-2 h-2 rounded-full ${p.status.includes('Open') || p.status.includes('Emergency') ? 'bg-emerald-500' : 'bg-slate-400'}"></span>
                            <span class="font-medium text-slate-700">${p.status}</span>
                        </div>

                        <div class="flex items-center space-x-2">
                            <button onclick="event.stopPropagation(); openDetailModal('${p.id}')" class="px-3 py-1.5 text-xs font-semibold text-teal-700 bg-teal-50 hover:bg-teal-100 rounded-lg transition border border-teal-200">
                                Details
                            </button>
                            <button onclick="event.stopPropagation(); triggerBookingModal('${p.id}')" class="px-3 py-1.5 text-xs font-semibold text-white bg-teal-600 hover:bg-teal-700 rounded-lg shadow-sm transition">
                                Book Appt
                            </button>
                        </div>
                    </div>
                </div>
            `;
            }).join('');
        }

        function selectPracticeCard(id) {
            activePracticeId = id;
            document.querySelectorAll('[id^="card-"]').forEach(c => c.classList.remove('ring-2', 'ring-teal-500', 'bg-teal-50/20'));
            
            const card = document.getElementById(`card-${id}`);
            if (card) {
                card.classList.add('ring-2', 'ring-teal-500', 'bg-teal-50/20');
            }

            const marker = markersMap[id];
            if (marker && map) {
                markerClusterGroup.zoomToShowLayer(marker, () => {
                    marker.openPopup();
                });
            }
        }

        function highlightCard(id) {
            // Find page index for card
            const index = filteredPractices.findIndex(p => p.id === id);
            if (index !== -1) {
                const targetPage = Math.floor(index / pageSize) + 1;
                if (targetPage !== currentPage) {
                    currentPage = targetPage;
                    renderPaginatedCards();
                }
                setTimeout(() => {
                    const card = document.getElementById(`card-${id}`);
                    if (card) card.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                }, 100);
            }
        }

        function prevPage() {
            if (currentPage > 1) {
                currentPage--;
                renderPaginatedCards();
                window.scrollTo({ top: 0, behavior: 'smooth' });
            }
        }

        function nextPage() {
            const totalPages = Math.ceil(filteredPractices.length / pageSize);
            if (currentPage < totalPages) {
                currentPage++;
                renderPaginatedCards();
                window.scrollTo({ top: 0, behavior: 'smooth' });
            }
        }

        function changePageSize() {
            pageSize = parseInt(document.getElementById('pageSize-select').value, 10);
            currentPage = 1;
            renderPaginatedCards();
        }

        function applyFilters() {
            const searchVal = document.getElementById('search-input').value.toLowerCase().trim();
            const countyVal = document.getElementById('county-select').value;
            const insuranceVal = document.getElementById('insurance-select').value;
            const minRatingVal = parseFloat(document.getElementById('rating-select').value);
            const emergencyOnly = document.getElementById('toggle-emergency').checked;
            const weekendOnly = document.getElementById('toggle-weekend').checked;
            const newPatientOnly = document.getElementById('toggle-newpatient').checked;
            const sortVal = document.getElementById('sort-select').value;

            filteredPractices = allDentalPractices.filter(p => {
                // Search Query
                if (searchVal) {
                    const matchName = p.name.toLowerCase().includes(searchVal);
                    const matchAddress = p.address.toLowerCase().includes(searchVal);
                    const matchNeighborhood = p.neighborhood.toLowerCase().includes(searchVal);
                    const matchZip = p.zip.includes(searchVal);
                    const matchCounty = p.county.toLowerCase().includes(searchVal);
                    const matchDoctors = p.doctors.some(d => d.name.toLowerCase().includes(searchVal));
                    if (!matchName && !matchAddress && !matchNeighborhood && !matchZip && !matchCounty && !matchDoctors) return false;
                }

                // County Filter
                if (countyVal !== "ALL" && p.county !== countyVal) return false;

                // Insurance Filter
                if (insuranceVal !== "ALL" && !p.insurances.includes(insuranceVal)) return false;

                // Specialty Filter
                if (currentSpecialtyFilter !== "ALL" && !p.specialties.includes(currentSpecialtyFilter)) return false;

                // Minimum Rating
                if (minRatingVal > 0 && p.rating < minRatingVal) return false;

                // Feature Toggles
                if (emergencyOnly && !p.emergency) return false;
                if (weekendOnly && !p.weekend) return false;
                if (newPatientOnly && !p.acceptingNew) return false;

                return true;
            });

            // Sorting Logic
            if (sortVal === "rating-desc") {
                filteredPractices.sort((a, b) => b.rating - a.rating || b.reviews - a.reviews);
            } else if (sortVal === "reviews-desc") {
                filteredPractices.sort((a, b) => b.reviews - a.reviews);
            } else if (sortVal === "name-asc") {
                filteredPractices.sort((a, b) => a.name.localeCompare(b.name));
            } else if (sortVal === "county-asc") {
                filteredPractices.sort((a, b) => a.county.localeCompare(b.county));
            }

            currentPage = 1;
            renderPaginatedCards();
            updateMapMarkers(filteredPractices);
            updateKPIs(filteredPractices);

            if (chartsInitialized) {
                updateAnalyticsCharts();
            }
        }

        function toggleSpecialty(specialty) {
            currentSpecialtyFilter = specialty;
            document.querySelectorAll('#specialty-chips button').forEach(btn => {
                if (btn.getAttribute('data-specialty') === specialty) {
                    btn.className = "chip-btn active-chip px-2.5 py-1 text-xs rounded-full border transition font-medium bg-teal-700 text-white border-teal-700";
                } else {
                    btn.className = "chip-btn px-2.5 py-1 text-xs rounded-full border bg-slate-100 text-slate-700 border-slate-200 hover:bg-slate-200 transition font-medium";
                }
            });
            applyFilters();
        }

        function handleSearch() {
            applyFilters();
        }

        function resetAllFilters() {
            document.getElementById('search-input').value = "";
            document.getElementById('county-select').value = "ALL";
            document.getElementById('insurance-select').value = "ALL";
            document.getElementById('rating-select').value = "0";
            document.getElementById('toggle-emergency').checked = false;
            document.getElementById('toggle-weekend').checked = false;
            document.getElementById('toggle-newpatient').checked = false;
            document.getElementById('sort-select').value = "rating-desc";
            toggleSpecialty('ALL');
            showToast("Filters reset to default view");
        }

        function toggleBookmark(id) {
            if (bookmarkedPractices.has(id)) {
                bookmarkedPractices.delete(id);
                showToast("Practice removed from bookmarks");
            } else {
                bookmarkedPractices.add(id);
                showToast("Practice saved to bookmarks");
            }
            renderPaginatedCards();
        }

        function updateKPIs(practices) {
            document.getElementById('kpi-total').innerText = practices.length.toLocaleString();
            document.getElementById('kpi-total-sub').innerText = practices.length === allDentalPractices.length 
                ? `100% Active Index` 
                : `${((practices.length / allDentalPractices.length) * 100).toFixed(1)}% of total metro`;
            
            if (practices.length > 0) {
                const avgRating = (practices.reduce((acc, curr) => acc + curr.rating, 0) / practices.length).toFixed(2);
                document.getElementById('kpi-rating').innerText = avgRating;
                const emergencyCount = practices.filter(p => p.emergency).length;
                document.getElementById('kpi-emergency').innerText = emergencyCount.toLocaleString();
                
                const counties = new Set(practices.map(p => p.county));
                document.getElementById('kpi-counties').innerText = `${counties.size} Counties`;
            } else {
                document.getElementById('kpi-rating').innerText = "0.0";
                document.getElementById('kpi-emergency').innerText = "0";
                document.getElementById('kpi-counties').innerText = "0 Counties";
            }
        }

        function openDetailModal(id) {
            const p = allDentalPractices.find(item => item.id === id);
            if (!p) return;
            activeModalPractice = p;

            document.getElementById('modal-title').innerText = p.name;
            document.getElementById('modal-county-badge').innerText = `${p.county} County • ${p.neighborhood}`;
            document.getElementById('modal-address').innerHTML = `<i class="fa-solid fa-location-dot text-teal-400"></i> ${p.address}`;
            document.getElementById('modal-rating').innerHTML = `${p.rating} <i class="fa-solid fa-star text-amber-500 text-sm"></i>`;
            document.getElementById('modal-reviews').innerText = p.reviews;
            document.getElementById('modal-status').innerText = p.status;
            document.getElementById('modal-phone').innerText = p.phone;

            // Render Doctors
            const doctorsContainer = document.getElementById('modal-doctors-list');
            doctorsContainer.innerHTML = p.doctors.map(d => `
                <div class="p-3 bg-slate-50 rounded-xl border border-slate-200 flex items-center space-x-3">
                    <div class="w-10 h-10 rounded-full bg-teal-100 text-teal-700 flex items-center justify-center font-bold text-sm">
                        <i class="fa-solid fa-user-doctor"></i>
                    </div>
                    <div>
                        <h5 class="font-bold text-xs text-slate-900">${d.name}</h5>
                        <p class="text-[11px] text-teal-700 font-medium">${d.role}</p>
                        <p class="text-[10px] text-slate-400">Exp: ${d.experience}</p>
                    </div>
                </div>
            `).join('');

            // Render Services
            const servicesContainer = document.getElementById('modal-services-table');
            servicesContainer.innerHTML = p.procedures.map(proc => `
                <tr class="hover:bg-slate-50">
                    <td class="p-2.5 font-medium text-slate-800">${proc.name}</td>
                    <td class="p-2.5"><span class="px-2 py-0.5 rounded bg-slate-100 text-slate-600 text-[10px] font-semibold">${proc.category}</span></td>
                    <td class="p-2.5 text-right font-bold text-teal-700">${proc.cost}</td>
                </tr>
            `).join('');

            // Render Hours
            const hoursContainer = document.getElementById('modal-hours-list');
            hoursContainer.innerHTML = Object.entries(p.hours).map(([day, hrs]) => `
                <li class="flex justify-between py-1 border-b border-slate-100">
                    <span class="font-semibold text-slate-700">${day}:</span>
                    <span class="text-slate-600">${hrs}</span>
                </li>
            `).join('');

            // Render Insurance Pills
            const insContainer = document.getElementById('modal-insurance-pills');
            insContainer.innerHTML = p.insurances.map(ins => `
                <span class="px-2.5 py-1 bg-teal-50 text-teal-800 border border-teal-200 rounded-lg text-xs font-semibold">${ins}</span>
            `).join('');

            switchModalTab('doctors');
            document.getElementById('detail-modal').classList.remove('hidden');
        }

        function closeDetailModal() {
            document.getElementById('detail-modal').classList.add('hidden');
        }

        function switchModalTab(tabKey) {
            ['doctors', 'services', 'hours'].forEach(key => {
                const tabEl = document.getElementById(`mtab-${key}`);
                const btnEl = document.getElementById(`mtab-${key}-btn`);
                if (key === tabKey) {
                    tabEl.classList.remove('hidden');
                    btnEl.className = "py-2 border-b-2 border-teal-600 text-teal-700 font-semibold";
                } else {
                    tabEl.classList.add('hidden');
                    btnEl.className = "py-2 border-b-2 border-transparent text-slate-500 hover:text-slate-700";
                }
            });
        }

        function copyAddressToClipboard() {
            if (!activeModalPractice) return;
            const textToCopy = `${activeModalPractice.name}, ${activeModalPractice.address}`;
            const dummy = document.createElement("textarea");
            document.body.appendChild(dummy);
            dummy.value = textToCopy;
            dummy.select();
            document.execCommand("copy");
            document.body.removeChild(dummy);
            showToast("Address copied to clipboard!");
        }

        function jumpToModalMarker() {
            if (!activeModalPractice) return;
            closeDetailModal();
            switchTab('directory');
            selectPracticeCard(activeModalPractice.id);
        }

        // Booking Modal
        function triggerBookingModal(id) {
            const p = allDentalPractices.find(item => item.id === id);
            if (p) activeModalPractice = p;
            openBookingModal();
        }

        function openBookingModal() {
            closeDetailModal();
            if (activeModalPractice) {
                document.getElementById('booking-practice-name').innerText = activeModalPractice.name;
            }
            document.getElementById('booking-modal').classList.remove('hidden');
        }

        function closeBookingModal() {
            document.getElementById('booking-modal').classList.add('hidden');
        }

        function handleBookingSubmit(event) {
            event.preventDefault();
            const name = document.getElementById('book-name').value;
            const practiceName = activeModalPractice ? activeModalPractice.name : 'Selected Dental Office';
            closeBookingModal();
            showToast(`Appointment requested for ${name} at ${practiceName}!`);
        }

        function exportToCSV() {
            if (filteredPractices.length === 0) {
                showToast("No data to export");
                return;
            }

            const headers = ["ID", "Name", "County", "City/Neighborhood", "Address", "Phone", "Rating", "Reviews", "Emergency 24/7", "Open Weekends", "Insurances"];
            const rows = filteredPractices.map(p => [
                p.id,
                `"${p.name.replace(/"/g, '""')}"`,
                `"${p.county}"`,
                `"${p.neighborhood}"`,
                `"${p.address.replace(/"/g, '""')}"`,
                `"${p.phone}"`,
                p.rating,
                p.reviews,
                p.emergency ? "Yes" : "No",
                p.weekend ? "Yes" : "No",
                `"${p.insurances.join(', ')}"`
            ]);

            const csvContent = "data:text/csv;charset=utf-8," 
                + [headers.join(','), ...rows.map(e => e.join(','))].join('\n');

            const encodedUri = encodeURI(csvContent);
            const link = document.createElement("a");
            link.setAttribute("href", encodedUri);
            link.setAttribute("download", `ATL_Dental_Offices_Export_${filteredPractices.length}.csv`);
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);

            showToast(`Exported ${filteredPractices.length} dental offices to CSV!`);
        }

        // Toast System
        function showToast(message) {
            const container = document.getElementById('toast-container');
            const toast = document.createElement('div');
            toast.className = "bg-slate-900 text-white px-4 py-3 rounded-xl shadow-xl text-xs font-semibold border border-slate-700 flex items-center space-x-2 animate-modal pointer-events-auto";
            toast.innerHTML = `
                <i class="fa-solid fa-circle-check text-teal-400 text-sm"></i>
                <span>${message}</span>
            `;
            container.appendChild(toast);
            setTimeout(() => {
                toast.style.opacity = '0';
                toast.style.transition = 'opacity 0.3s ease-out';
                setTimeout(() => toast.remove(), 300);
            }, 3000);
        }

        function switchTab(viewName) {
            const dirView = document.getElementById('view-directory');
            const anaView = document.getElementById('view-analytics');
            const dirBtn = document.getElementById('tab-directory-btn');
            const anaBtn = document.getElementById('tab-analytics-btn');

            if (viewName === 'directory') {
                dirView.classList.remove('hidden');
                anaView.classList.add('hidden');
                dirBtn.className = "px-3 py-1.5 rounded-md bg-teal-600 text-white font-semibold shadow-sm transition-all flex items-center gap-1.5";
                anaBtn.className = "px-3 py-1.5 rounded-md text-slate-300 hover:text-white hover:bg-slate-700/50 transition-all flex items-center gap-1.5";
                setTimeout(() => { if (map) map.invalidateSize(); }, 200);
            } else {
                dirView.classList.add('hidden');
                anaView.classList.remove('hidden');
                anaBtn.className = "px-3 py-1.5 rounded-md bg-teal-600 text-white font-semibold shadow-sm transition-all flex items-center gap-1.5";
                dirBtn.className = "px-3 py-1.5 rounded-md text-slate-300 hover:text-white hover:bg-slate-700/50 transition-all flex items-center gap-1.5";
                initAnalyticsCharts();
            }
        }

        // Analytics Charts Initializer
        let chartsInitialized = false;
        let chart1Instance = null;
        let chart2Instance = null;
        let chart3Instance = null;
        let chart4Instance = null;

        function initAnalyticsCharts() {
            if (chartsInitialized) {
                updateAnalyticsCharts();
                return;
            }
            chartsInitialized = true;

            // Chart 1: Practice Count by County
            chart1Instance = new Chart(document.getElementById('chart-county-dist'), {
                type: 'doughnut',
                data: getCountyDistData(),
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: { legend: { position: 'right', labels: { font: { size: 11 } } } }
                }
            });

            // Chart 2: Average Rating by Metro County
            chart2Instance = new Chart(document.getElementById('chart-county-ratings'), {
                type: 'bar',
                data: getCountyRatingData(),
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: { y: { min: 4.0, max: 5.0 } }
                }
            });

            // Chart 3: Top Accepted Insurances
            chart3Instance = new Chart(document.getElementById('chart-insurance'), {
                type: 'bar',
                data: getInsuranceDistData(),
                options: {
                    indexAxis: 'y',
                    responsive: true,
                    maintainAspectRatio: false
                }
            });

            // Chart 4: Service Availability
            chart4Instance = new Chart(document.getElementById('chart-availability'), {
                type: 'bar',
                data: getAvailabilityData(),
                options: {
                    responsive: true,
                    maintainAspectRatio: false
                }
            });
        }

        function updateAnalyticsCharts() {
            if (!chartsInitialized) return;

            chart1Instance.data = getCountyDistData();
            chart1Instance.update();

            chart2Instance.data = getCountyRatingData();
            chart2Instance.update();

            chart3Instance.data = getInsuranceDistData();
            chart3Instance.update();

            chart4Instance.data = getAvailabilityData();
            chart4Instance.update();
        }

        function getCountyDistData() {
            const countyDist = {};
            filteredPractices.forEach(p => {
                countyDist[p.county] = (countyDist[p.county] || 0) + 1;
            });

            return {
                labels: Object.keys(countyDist).map(c => `${c} County`),
                datasets: [{
                    data: Object.values(countyDist),
                    backgroundColor: ['#0d9488', '#06b6d4', '#3b82f6', '#8b5cf6', '#f59e0b', '#ec4899', '#10b981']
                }]
            };
        }

        function getCountyRatingData() {
            const countyRatings = {};
            const countyCounts = {};
            filteredPractices.forEach(p => {
                countyRatings[p.county] = (countyRatings[p.county] || 0) + p.rating;
                countyCounts[p.county] = (countyCounts[p.county] || 0) + 1;
            });
            const countyAvgs = Object.keys(countyRatings).map(c => (countyRatings[c] / countyCounts[c]).toFixed(2));

            return {
                labels: Object.keys(countyRatings).map(c => `${c} County`),
                datasets: [{
                    label: 'Avg Patient Rating',
                    data: countyAvgs,
                    backgroundColor: '#0f766e',
                    borderRadius: 6
                }]
            };
        }

        function getInsuranceDistData() {
            const insuranceCounts = {};
            filteredPractices.forEach(p => {
                p.insurances.forEach(i => {
                    insuranceCounts[i] = (insuranceCounts[i] || 0) + 1;
                });
            });

            return {
                labels: Object.keys(insuranceCounts),
                datasets: [{
                    label: 'Practices Accepting Network',
                    data: Object.values(insuranceCounts),
                    backgroundColor: '#0891b2',
                    borderRadius: 6
                }]
            };
        }

        function getAvailabilityData() {
            const emergencyCount = filteredPractices.filter(p => p.emergency).length;
            const weekendCount = filteredPractices.filter(p => p.weekend).length;
            const newPatientsCount = filteredPractices.filter(p => p.acceptingNew).length;

            return {
                labels: ['Emergency 24/7', 'Open Weekends', 'Accepting New Patients'],
                datasets: [{
                    label: 'Number of Dental Offices',
                    data: [emergencyCount, weekendCount, newPatientsCount],
                    backgroundColor: ['#e11d48', '#0d9488', '#2563eb'],
                    borderRadius: 6
                }]
            };
        }

        window.onload = function() {
            initMap();
            applyFilters();
        };
    </script>
</body>
</html>
